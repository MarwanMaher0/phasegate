# Requirements

<!--
phasegate guidance: REQUIREMENTS (small profile)
Three to seven requirements is plenty. Each gets a heading starting with REQ-n
and at least one Given / When / Then. Example, to copy outside this comment:

## REQ-1 Import a bank statement

- Given a CSV export from my bank
  When I drop it on the import page
  Then each transaction appears once, with duplicates skipped

Why this phase exists: npx phasegate explain requirements
-->
